exemple utilisation :
./main --abr -f test3.csv -o out.csv -r

voir code d'erreur ensuite :
echo $?

compilation:

make

ou

make && ./main

ou (force à recompiler si changement pas trouvé)

make -B && ./main
