exemple utilisation :
./main.sh --abr -f meteo.csv -o out.csv -r -m

pour les modes de la pression et de la température, il faut espacer t (ou p) puis le mode :
exemples : ./main.sh --abr -f meteo.csv -o out.csv -r -t 1
          ./main.sh --abr -f meteo.csv -o out.csv -r -p 3


voir code d'erreur ensuite :
echo $?

compilation:

make

ou (force à recompiler si changement pas trouvé)

make -B 
